% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opsr_profile_rho.R
\name{opsr_profile_rho}
\alias{opsr_profile_rho}
\title{Profile Likelihood for the Rho Parameters}
\usage{
opsr_profile_rho(
  object,
  grid = seq(-0.99, 0.99, length.out = 21),
  printLevel = 0,
  ...
)
}
\arguments{
\item{object}{an object of class \code{"opsr"}.}

\item{grid}{numeric vector of \eqn{\rho} values in \eqn{(-1, 1)} at which
to evaluate the profile.  Defaults to 21 equally-spaced points.}

\item{printLevel}{integer passed to \code{\link{opsr}} for each profile evaluation.
Defaults to \code{0} (silent).}

\item{...}{further arguments passed to \code{\link{opsr}} for each profile evaluation.}
}
\value{
A \code{data.frame} of class \code{"opsr.profile.rho"} with columns
\code{regime} (integer), \code{rho} (fixed value), and \code{profile_loglik}
(maximised log-likelihood with \eqn{\rho_j} fixed).  Failed evaluations
are recorded as \code{NA}.  Attributes \code{rho_hat} and \code{max_loglik} store the
MLE estimates and the unrestricted maximum for reference.
}
\description{
For each regime \eqn{j}, evaluates the profile log-likelihood by fixing
\eqn{\rho_j} on a grid of values and re-maximising over all remaining
parameters.  A flat or bimodal profile indicates that the data do not
strongly identify \eqn{\rho_j}, implying that the Heckman correction may
rely on functional-form assumptions (normality) rather than a genuine
exclusion restriction.
}
\seealso{
\code{\link{opsr_test_exclusion_restriction}}, \code{\link{print.opsr.profile.rho}},
\code{\link{plot.opsr.profile.rho}}
}
