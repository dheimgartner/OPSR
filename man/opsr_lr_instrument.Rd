% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opsr_lr_instrument.R
\name{opsr_lr_instrument}
\alias{opsr_lr_instrument}
\title{LR Test for Instrument Exclusion from Selection Equation}
\usage{
opsr_lr_instrument(object, instrument, printLevel = 0, ...)
}
\arguments{
\item{object}{an object of class \code{"opsr"}.}

\item{instrument}{character vector naming the selection-equation variable(s)
to exclude.  Must match variable names as they appear after the \code{s_}
prefix in \code{coef(object)} (i.e., without the prefix).}

\item{printLevel}{integer passed to \code{\link{opsr}} when refitting the restricted
model.  Defaults to \code{0} (silent).}

\item{...}{further arguments passed to \code{\link{opsr}} when refitting.}
}
\value{
An object of class \code{"anova.opsr"} as returned by \code{\link{anova.opsr}},
comparing the restricted model (instrument fixed at zero) to the full
model.
}
\description{
Re-fits a restricted model with one or more selection-equation variables
fixed at zero and compares it to the full model via \code{\link{anova.opsr}}.
A small LR statistic (or large p-value) means the instrument contributes
little information to the selection equation — a sign of weak identification.
}
\seealso{
\code{\link{anova.opsr}}, \code{\link{opsr_test_exclusion_restriction}}
}
